spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: tester
    password: 12345

  jpa:
    hibernate:
      ddl-auto: create-drop
    open-in-view: false

  sql:
    init:
      # <-- Add this to prevent schema.sql/data.sql execution
      mode: never  

logging:
  level:
    root: WARN
    '[com.demo.tasktracker]': INFO